{
    
   "endpoints": [
        {
            "id": "endpoint-001",
            "endpoint": "getSlashRoot",
            "BASE_URL": "http://localhost:3000",
            "root": "/",
            "httpMethod": "GET",
            "response":"api/JSON", 
            "status_code":200,
            "description": "Endpoint com o metodo http GET , status code 200 e que corresponde a root '/' e que exibe um este JSON com endpoints construidos" 
        }
        ,
        {
            "id": "endpoint-002",
            "endpoint": "getSlashRoot",
            "BASE_URL": "http://localhost:3000",
            "root": "/",
            "httpMethod": "GET",
            "response":"api/JSON", 
            "status_code":200,
            "description": "Endpoint com o metodo http GET , status code 200 e que corresponde a root '/' e que exibe um este JSON com endpoints construidos" 
        }
    ]
}